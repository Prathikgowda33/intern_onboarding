# Prompt Log

## Prompt 1
Create a Python command-line tool named md2html.py that converts Markdown to HTML.

## Prompt 2
Improve the parser to support headings, bold, italic, inline code, links, lists, blockquotes, horizontal rules, and fenced code blocks.

## Prompt 3
Review the generated code and ensure it uses argparse, handles missing files gracefully, and writes valid HTML output.
