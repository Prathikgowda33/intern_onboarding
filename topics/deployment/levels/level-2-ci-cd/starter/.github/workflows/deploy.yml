# GitHub Actions workflow for Deployment Level 2
# TODO: Complete this workflow to build and deploy on push to main

name: Deploy to Railway

on:
  push:
    branches:
      # TODO: Which branch should trigger this workflow?
      - main

jobs:
  # TODO: Name this job (e.g., build-and-deploy)
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # TODO: Step 1 — Checkout the repository code
      # - uses: actions/checkout@v4

      # TODO: Step 2 — Set up Docker Buildx
      # - uses: docker/setup-buildx-action@v3

      # TODO: Step 3 — Log in to GitHub Container Registry
      # - uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}

      # TODO: Step 4 — Build and push Docker image
      # - uses: docker/build-push-action@v5
      #   with:
      #     push: true
      #     tags: ghcr.io/${{ github.repository }}:latest
      #     build-args: |
      #       COMMIT_HASH=${{ github.sha }}

      # TODO: Step 5 — Deploy to Railway
      # Option A: Use Railway CLI
      # - run: |
      #     npm install -g @railway/cli
      #     railway up --service ${{ secrets.RAILWAY_SERVICE_ID }}
      #   env:
      #     RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      #
      # Option B: Configure Railway to auto-deploy from GitHub
      # (simpler — just set up Railway to watch the repo)
      # Then this workflow just needs to build and verify

      # TODO: Step 6 — Verify deployment (optional but recommended)
      # - run: curl -s https://<your-url>/version
